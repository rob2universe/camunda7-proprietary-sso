camunda:
  bpm:
    admin-user:
      id: demo
      password: demo

    filter:
      create: All tasks
    authorization:
      enabled: true

spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: 'jdbc:h2:file:./camunda-h2-database'
    username: sa
    password: sa

server:
  port: 8080

